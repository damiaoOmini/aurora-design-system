# Patterns

Reusable interface and product patterns built from Aurora foundations, tokens and components.

Patterns will document recurring solutions such as navigation, forms, filtering, tables, feedback, empty states and product-specific flows.

Pattern documentation will be added only after the underlying components and rules are understood.
