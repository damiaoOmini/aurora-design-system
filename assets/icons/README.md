# Aurora Icons

This directory represents Aurora's approved icon asset inventory.

- `manifest.json` records the current Figma source, inventory boundary, domain-specific sets and consumption constraints.
- Vector geometry is not duplicated in the repository in v0.
- Figma remains the asset source for individual icon vectors until a code distribution/export strategy is defined.

See `foundations/iconography.md` for iconography principles and AI usage rules.