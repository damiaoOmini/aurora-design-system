# Component Previews

This directory contains visual evidence linked to Aurora Component Contracts.

Each component preview should live in its own directory and, when possible, include:

- `reference.png` — approved static visual reference.
- `README.md` — source, purpose, related contract and limitations.

A component preview must not replace the corresponding `.json` or `.md` contract.
