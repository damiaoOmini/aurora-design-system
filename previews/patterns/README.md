# Pattern Previews

This directory is reserved for visual evidence linked to Aurora Pattern Contracts.

Pattern previews should demonstrate approved composition without redefining component appearance. They must reference existing Component Contracts, tokens and assets instead of visually approximating missing dependencies.
